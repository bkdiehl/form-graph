node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Vendored civitai v1 code + its differential-parity apparatus — local-only,
# used to prove the src/lib/civitai ports against the verbatim originals.
# Never committed: the civitai repo is the home of that code.
/src/v1
/tsconfig.v1.json
/scripts/extract-v1-tables.mjs
/build
